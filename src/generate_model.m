function generate_model(hObject,eventdata,handles)
% Regenerate the model with current settings
% project(hObject,eventdata,handles);
% Recreate model
mesher(hObject,eventdata,handles)
% plot_site(hObject,eventdata,handles,'noname');
return


